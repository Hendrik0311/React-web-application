# React-web-application
In this project I created a React web application that uses React, JSX and Javascript to create a Web Store.

Using two commands you can install the node modules that are necessary to run the application.
Two commands are npm install and npm start to run the application.
